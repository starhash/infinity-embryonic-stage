<UserControl x:Name="userControl" x:Class="InfinityIDE.IconMenuItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="64" FontSize="11" Foreground="White">
    <Grid Background="{Binding Background, ElementName=userControl}">
        <ContentControl x:Name="ContentView" Content="" Margin="0,0,0,32" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontFamily="Segoe UI Symbol" Padding="0"/>
        <Label x:Name="LabelView" Content="Label&#xD;&#xA;Presenter" Padding="0" FontSize="{Binding FontSize, ElementName=userControl}" Margin="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" VerticalAlignment="Bottom" Height="32" FontFamily="{Binding FontFamily, ElementName=userControl}" Foreground="{Binding Foreground, ElementName=userControl}"/>

    </Grid>
</UserControl>
